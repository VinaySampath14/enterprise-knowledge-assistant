services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: enterprise-knowledge-assistant:latest
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    volumes:
      - ./logs:/app/logs
